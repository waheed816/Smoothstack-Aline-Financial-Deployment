import { FormControlLabelPipe } from './form-control-label.pipe';

describe('FormControlLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new FormControlLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
