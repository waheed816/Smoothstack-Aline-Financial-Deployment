import {AccountLabelPipe} from './account-label.pipe';

describe('AccountLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
